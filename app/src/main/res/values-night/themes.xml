<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Bookify" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimaryContainer">@color/dark_grey</item>
        <item name="colorOnPrimaryContainer">@color/honeydew</item>

        <item name="colorSecondary">@color/dark_grey</item> <!--light_grey -->
        <item name="colorOnSecondary">@color/off_white</item>
        <item name="iconTint">@color/honeydew</item>
        <item name="textFillColor">@color/white</item>
        <item name="colorOnBackground">@color/lemon</item>

        <item name="colorPrimary">@color/off_white</item>
        <item name="colorPrimaryDark">@color/dark_grey</item>

        <item name="android:windowBackground">@color/dark_background</item>
        <item name="popupMenuBackground">@color/grey</item>
        <item name="colorOutline">@color/battleship</item>

        <item name="android:fontFamily">@font/montserrat</item>

        <item name="logo">@drawable/logo</item>
    </style>

    <style name="Theme.Bookify" parent="Base.Theme.Bookify" />

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_in</item>
        <item name="android:windowExitAnimation">@anim/slide_out</item>
    </style>
</resources>